[vars]
container_tag = "${TAG}"

[tasks.ci]
depends = ["build","lint","test"]
run = 'docker buildx build -f Containerfile.ci --platform linux/arm64,linux/amd64 -t git.saintnet.tech/stryan/materia:{{vars.container_tag}} .'
